<%= form_with model: @recipe_ingredient, :url => recipe_recipe_ingredients_path do |f| %>


    <%= f.collection_select :recipe_id, Recipe.all, :id, :name %>
    
        <%= f.label :instruction %>
        <%= f.text_field :instruction %>
       
    <%= f.collection_select :ingredient_id, Ingredient.all, :id, :name %>
    
        <br><br>

            <%= f.submit %>

        <br><br>
<% end %>